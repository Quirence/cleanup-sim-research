# Follow-up к ревью репозитория от 2026-08-12

Дата: 2026-08-16

Источник ревью: `docs/project/repository_review_2026-08-12.md`.

Цель: зафиксировать текущее состояние после merge ветки `dev`, локальной
проверки тестов и дополнительного исправления документации/тестового ожидания.

## Что проверено

- Актуальная merged-ветка `origin/main` на commit `cbb3bd9`.
- Результаты pre-final серии:
  `out/cleanup_sim_v2/prefinal_10seed_all_scenarios_2026-08-13/summary.csv`.
- Текущие файлы `cleanup_sim_v2/config.py`, `run_experiments.py`,
  `simulation.py`, `planners.py`.
- Документы `README.md`, `README_WORKFLOW.md`, `docs/project/current_state.md`,
  `docs/article/stage1/`, `docs/article/stage2/`.

## Дополнительные исправления этого follow-up

| Находка | Статус | Действие |
|---|---|---|
| Локально падал post-review тест `test_belief_horizon_scores_swept_density_without_truth_access` | исправлено | Ожидание `candidate_type` приведено к фактическому поведению `density_transect`: тест остается строгим и проверяет swept-density выбор без доступа к истинному полю. |
| `README.md` содержал устаревшее `123 passed` | исправлено | Обновлено до актуального полного набора `161 passed`. |
| `docs/project/current_state.md` содержал устаревшие дату, тестовый счетчик и узкий научный фокус | исправлено | Обновлен статус на 2026-08-16, добавлена текущая рамка adaptive mission planning и ссылка на новую постановку. |
| `README_WORKFLOW.md` называл рабочий вектор слишком узко через вероятностное планирование | исправлено | Рабочий вектор расширен до адаптивного планирования миссии при неполной информации. |
| `docs/article/stage1/stage2` не были синхронизированы с `cleanup_sim_v2` и pre-final результатами | исправлено навигационно | Добавлен актуальный файл `stage1_reframing_after_prefinal_2026-08-16.md`; старые stage1/stage2 документы помечены как исторические/частично устаревшие. |
| `belief_cluster_route` писал собственные route-действия как `belief_horizon` | исправлено | `belief_cluster_route` теперь сохраняет собственный `mode` в `GoalDecision`/events; общая физика belief-целей расширена на этот mode, поэтому поведение движения сохраняется. |

## Статус пунктов исходного ревью

| Пункт ревью | Текущий статус |
|---|---|
| Critical: абляционные тесты проверяли конфиг, а не поведение | закрыто в `912d45a`; текущий полный pytest проходит после follow-up фикса. |
| Important #1: `next_active` fallback использовал дефолтный platform/state | закрыто в `1b50b3e`. |
| Important #2: дублированные magic weights в scoring | закрыто в `e2ed2d6` и `2b4c82d`. |
| Important #3: длинный `run_simulation` | частично закрыто в `3008575`; summary вынесен. Оставшийся sensing/goal-completion рефакторинг является maintainability-задачей, не блокирует корректность текущих результатов. |
| Important #4: `belief_cluster_route`/`belief_orienteering` завязаны на `next_belief_horizon` fallback | концептуальный fallback пересмотрен как архитектурное решение opportunity-cost; безопасная диагностическая часть закрыта: `belief_cluster_route` больше не маркирует свои route-действия как `belief_horizon`. |
| Important #5: непокрытые модули v2 | закрыто прямыми unit/CLI тестами в `30c43d0`, `f7c2bb3`, `9f15cf5`, `59864d2`, `503a673`. |
| Important #6: README/Workflow устарели | закрыто, дополнительно актуализировано в этом follow-up. |
| Important #7: stage1/stage2 не связаны с v2 | закрыто навигационно: добавлен актуальный reframing-файл и notices. Содержательная переработка всей статьи остается следующим редакционным этапом. |
| Important #8: CI не запускался на `dev` | закрыто в `1a9e0f2`. |
| Minor: рыхлые assert-ы | закрыто в `3c3cd24`; дополнительный post-review assert исправлен в этом follow-up. |
| Minor: `PlannerConfig` плоский и большой | осознанно deferred; это рефакторинг API, не текущая ошибка корректности. |
| Minor: `GoalDecision.expected_value` двусмысленен | закрыто поясняющей docstring-заметкой в `2b4c82d`. |
| Minor: `docs/project` содержит дневниковые отчеты | частично закрыто historical-пометками в `d915f60`; новые отчеты сохраняются как traceability. |

## Текущее состояние

Вердикт по ревью репозитория:

> Корректностные и воспроизводимые ошибки ревью закрыты. Остались осознанно
> отложенные архитектурные рефакторинги (`run_simulation`, вложенный
> `PlannerConfig`) и содержательная переработка статьи после выбора финального
> алгоритмического направления.

Текущий научный центр после follow-up:

> адаптивное планирование миссии поиска и сбора плавающего мусора при неполной,
> шумной и устаревающей информации о целях, физически ограниченном сборе и
> дрейфе объектов.

## Проверка

Команда:

```powershell
python -m pytest -q
```

Результат:

```text
161 passed
```

Примечание: строка `123 passed` остается только в
`repository_review_fix_plan_2026-08-13.md` как историческая отметка результата
после волны 1, а не как актуальное ожидаемое состояние проекта.
